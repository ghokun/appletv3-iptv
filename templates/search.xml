{{ define "body" -}}
<search id="{{ .BodyID }}">
  <header>
    <simpleHeader accessibilityLabel="{{ index .Translations "search.title" }}">
      <title>{{ index .Translations "search.title" }}</title>
    </simpleHeader>
  </header>
  <baseURL>{{ .BasePath }}/search-results.xml?term=</baseURL>
</search>
{{- end }}